# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="postgresql://postgres:rootpassword@localhost:5432/quanlykho?schema=public"
JWT_SECRET="your_jwt_secret_key_here_secure_it_please"
PORT=3000

# Auth Configuration
DEFAULT_ADMIN_PASSWORD="123456"
JWT_EXPIRES_IN="24h"
BE_HOST="http://localhost:3000"

# --- Email Configuration ---
# Cách 1: Sử dụng Resend API (Khuyên Dùng - Tốc độ cao)
RESEND_API_KEY="re_4MY5XGgZ_7RuVyD2PewH1WMfmYoZRLUzy"
SMTP_FROM="onboarding@resend.dev"

# Cách 2: Sử dụng SMTP Truyền thống (Gmail, Sendgrid...) - Xoá (hoặc comment) biến RESEND_API_KEY ở trên để dùng cách 2
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_SECURE="false"
# SMTP_USER="vnphamhuyit9x@gmail.com"
# SMTP_PASS="tywx cuhc leqn rmzj"
# SMTP_FROM="vnphamhuyit9x@gmail.com"